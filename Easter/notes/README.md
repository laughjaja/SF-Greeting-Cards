# Easter Notes

- `app/` is the live Easter web card project and hosted asset source.
- `email/` contains the sendable Easter email template and media exports.
- Keep future Easter revisions here instead of mixing them into the repo root.
